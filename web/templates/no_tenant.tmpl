{{define "content"}}
<section class="hero">
  <div class="hero-copy">
    <span class="eyebrow">GoUp</span>
    <h1>Kein Tenant ausgewählt</h1>
    <p class="muted">Bitte gib den Tenant-Namen direkt in der URL an, z.B. <strong>/mein-tenant/</strong>.</p>
  </div>
</section>
{{end}}
